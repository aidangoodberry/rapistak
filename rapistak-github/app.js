const toggle=document.querySelector('.menu'),nav=document.querySelector('#nav'),dropdowns=[...document.querySelectorAll('.dropdown')];
function setDropdown(item,open){item.querySelector('button').setAttribute('aria-expanded',String(open));item.querySelector('.dropdown-panel').hidden=!open}
function closeDropdowns(except){dropdowns.forEach(item=>{if(item!==except)setDropdown(item,false)})}
function closeNavigation(){nav.classList.remove('open');toggle.setAttribute('aria-expanded','false');toggle.textContent='Menu';closeDropdowns()}
toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')!=='true';closeDropdowns();toggle.setAttribute('aria-expanded',String(open));nav.classList.toggle('open',open);toggle.textContent=open?'Close':'Menu'});
dropdowns.forEach(item=>{const button=item.querySelector('button');button.addEventListener('click',()=>{const open=button.getAttribute('aria-expanded')!=='true';closeDropdowns(item);setDropdown(item,open)});button.addEventListener('keydown',event=>{if(event.key==='ArrowDown'){event.preventDefault();closeDropdowns(item);setDropdown(item,true);item.querySelector('a').focus()}});item.addEventListener('focusout',()=>{setTimeout(()=>{if(!item.contains(document.activeElement))setDropdown(item,false)},0)})});
nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeNavigation));document.addEventListener('click',event=>{if(!nav.contains(event.target)&&!toggle.contains(event.target))closeNavigation()});document.addEventListener('keydown',event=>{if(event.key!=='Escape')return;const open=dropdowns.find(item=>item.querySelector('button').getAttribute('aria-expanded')==='true');if(open){setDropdown(open,false);open.querySelector('button').focus()}else if(nav.classList.contains('open')){closeNavigation();toggle.focus()}});
